To create HTTP + SOCKS5 Anonymous Proxies in a single server


On any Ubuntu/Debian server:


## Installation

Install my-project with npm

```bash
sudo bash -c "$(wget -qO- https://raw.githubusercontent.com/tackleberrybey/ipv4/refs/heads/main/setup-proxy.sh)"
```
    
